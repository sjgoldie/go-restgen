meta {
  name: Verify Delete Audit Log
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/audit-logs
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body[0].job_id: eq {{jobId}}
  res.body[0].operation: eq delete
  res.body[0].old_data: contains running
  res.body[0].new_data: eq
}
