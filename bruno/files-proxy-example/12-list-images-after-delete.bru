meta {
  name: List Images After Delete
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/posts/{{postId}}/images
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.data: isArray
  res.body.data.length: eq 1
}
