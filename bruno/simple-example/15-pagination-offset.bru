meta {
  name: Pagination with Offset
  type: http
  seq: 15
}

get {
  url: {{baseUrl}}/users?limit=1&offset=1&sort=Name
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body: isArray
  res.body: length 1
  res.body[0].name: eq Charlie Brown
}
