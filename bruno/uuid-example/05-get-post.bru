meta {
  name: Get Post by UUID
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/blogs/{{blogId}}/posts/{{postId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: eq {{postId}}
  res.body.blog_id: eq {{blogId}}
  res.body.title: eq First Post
}
